 <% if admin_signed_in? %>
 <div class="col-md-8 offset-md-2">
      <h3><span class="badge badge-light">会員一覧</span></h3>
      <table class="table table-hover table-inverse">
       <thead>
        <tr class="bg-light">
          <th>会員ID</th>
          <th>氏名</th>
          <th>メールアドレス</th>
          <th>ステータス</th>
        </tr>
      </thead>
       <% @end_users.each do |end_user| %>
      <tbody>
       <tr>
        <td><%= end_user.id %></td>
        <td><%= link_to end_user.name_last+end_user.name_first, admin_end_user_path(end_user) %></td>
        <td><%= end_user.email %></td>
        <td ><%= end_user.is_deleted ? '退会' : '有効' %></td>
       </tr>
      </tbody>
      <% end %>
      </table>
      <%= paginate(@end_users) %>
    </div>
<% else %>
  管理者専用ページです。閲覧のためにはログインが必要になります。
<% end %>